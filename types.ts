export interface GameError {\n    code: string;\n    message: string;\n    timestamp: Date;\n}\n\nexport function handleGameError(error: GameError): void {\n    console.error(`Error Code: ${error.code}`);\n    console.error(`Error Message: ${error.message}`);\n    console.error(`Timestamp: ${error.timestamp.toISOString()}`);\n    // Additional error handling logic can be implemented here\n    // e.g., displaying a user-friendly message, logging to an external service\n}\n\nexport function assertGameCondition(condition: boolean, message: string): void {\n    if (!condition) {\n        const error: GameError = {\n            code: 'GAME_CONDITION_FAILED',\n            message,\n            timestamp: new Date()\n        };\n        handleGameError(error);\n    }\n}\n\nexport type GameState = 'loading' | 'playing' | 'paused' | 'gameOver';\n\nexport interface Player {\n    id: string;\n    name: string;\n    score: number;\n}\n\nexport function createPlayer(name: string): Player {\n    if (!name || name.length === 0) {\n        const error: GameError = {\n            code: 'INVALID_PLAYER_NAME',\n            message: 'Player name cannot be empty',\n            timestamp: new Date()\n        };\n        handleGameError(error);\n        throw new Error('Player creation failed');\n    }\n    return {\n        id: crypto.randomUUID(),\n        name,\n        score: 0\n    };\n}